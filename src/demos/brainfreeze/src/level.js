/**
 * @typedef {Object} Level
 * @property {number[]} tiles
 * @property {number} colsPerRow
 * @property {[number, number]} player
 * @property {Array<[number, number]>} boxes
 * @property {Array<[number, number]>} goals
 */

export {};
