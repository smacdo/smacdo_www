+++
title = "Typography Test"
date = 2026-01-01
+++

A paragraph of body text. Reading comfort is the goal — warm ink on paper, generous line spacing, and a comfortable 65-character measure.

## Heading Two

Another paragraph with **bold** and _italic_ and `inline code`. The measure keeps lines short enough to read without losing your place.

### Heading Three

- List item one
- List item two
- List item three

```rust
fn main() {
    let x = 42;
    println!("Hello, world: {}", x);
}
```

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```
